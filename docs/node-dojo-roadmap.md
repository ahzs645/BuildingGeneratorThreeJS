# Node Dojo browser-port roadmap

Inventory date: 2026-07-12; parity status reconciled 2026-07-15. The source pack contains 16 `.blend` projects.

## Working or represented in the web app

- Recursive Bin Generator — live GN-VM, Blender comparison, baked variants.
- Chrome Crayon — live GN-VM, editable graph, reconstructed shader.
- Simple Bubble Vessel — live GN-VM and Blender comparison.
- Math Clay — all thirteen distinct surface roots run live from one portable scene dump; twelve are topology-exact and TPMS.016 is native-OpenVDB-equivalent within its observed scheduling range.
- Send Nodes Hat — all four distinct active roots now run live and are topology-exact. Typed/derived Object Info dependencies, evaluated-cycle guarding, and Blender-compatible per-spline Length sampling reproduce embroidery at 188,934 / 188,160 without embedding the final mesh.
- Procedural Typewriter — live GN-VM with editable text and scene-frame animation. The recovered original `Blurmed.ttf` supplies an embedded 96-glyph evaluated-outline atlas (ASCII plus the default text's em dash) without publishing the commercial binary; the page accepts the user's local TTF for editor-preview matching and retains Pixels as its legal display fallback. The frame-240 browser result is now topology-and-layout exact at 4,743 vertices / 33 faces, with matching wrapping, cursor placement, and world bounds within 0.00001 units.
- Chrome Asset Library: Periodic Brush — live GN-VM with all nine `period pack` collection children, editable distance/scale controls, and exact Blender parity across the validation sweep.
- Chrome Asset Library: Flat Stickie Pack — Blender reference and live GN-VM output match at 28 vertices, 7 faces, and identical local bounds.
- Chrome Asset Library: Polarity Sticker — Arc/Fill Curve output matches Blender at 68 vertices, 4 faces, and identical local bounds.
- Chrome Asset Library: Period Pack Shape — the shared `Geometry Nodes.004` root matches Blender at 28 vertices, 1 face, and identical local bounds; its nine object users are covered by this family.
- Chrome Asset Library: Sticker Noodle Brush — nested Polarity Sticker evaluation and Rotate Instances match Blender across spacing, size, and twist cases (16,252 vertices / 956 faces authored).
- Chrome Asset Library: Sticker Noodle Star — the shared `dot periodic brush.003` family used by eight objects matches Blender across spacing, star radii, and point-count cases (716,079 vertices / 715,520 faces authored).
- Chrome Asset Library: `gn.sticker control` and `gn.sticker control.001` — the 10pt Spoke and Soft Star flat-stickie roots match their Blender quads exactly (4 vertices / 1 face each).
- Chrome Asset Library: Outline Sticker — the `Geometry Nodes.009` root matches Blender across authored scale and outline controls (1,056 vertices / 1,025 faces).
- Chrome Asset Library: Image Pixel Stippler — packed image extraction, image sampling, field memoization, and BVH raycasting match Blender across fifteen geometry cases (72,094 vertices / 71,550 faces authored). A reproducible Eevee fixture renders the packed authored shader, and direct Blender/source diagnostics now drive planar Generated Z, signed PCG3D, and exact Greater Than semantics in WebGL. A 4x4 pixel footprint filters only the final authored binary result. Aligned 768px evidence improves to 0.9957 silhouette IoU, 0.3028 binary-mask disagreement, 0.1742 pixel correlation, and 0.2985 luminance MAE, so the remaining renderer-sampling/precision result is still a reconstruction rather than shader parity; normalized geometry diagnostics remain available.
- Chrome Asset Library: UI Window Generator — nested Object Info dependencies, relative transforms, boundary-only Mesh to Curve selection, vector-to-scalar socket conversion, and the recovered Pixels Medium font run in-browser. The page loads the supplied `pixels.ttf`, reports whether it is available, and falls back to the embedded Blender-extracted outline atlas when it is not. Grid-corner preservation, Blender-style bridge collapse, the extracted active-relative matrix, and Blender's 0.01 Mesh-to-Curve bound width make all three subdivision sweeps topology-exact with bounds within 0.0001 units, including the authored 35,886 / 33,474 result after 334 Blur Attribute passes.
- Chrome Asset Library: Chain and Mace — nested baked payloads, instance-domain separation, NURBS/resample tangent frames, curve reversal, rotation fields, and both endpoint/ball switches run live. All five validation cases now match Blender at 120,727 vertices / 214,718 faces, with local bounds within 0.0001 units.
- Chrome Asset Library: Mesh to Curve Helper — `Geometry Nodes.014` exactly preserves the authored quad as one cyclic four-point curve. It intentionally renders no surface (`0 / 0`) in both Blender and the browser because no bevel or profile follows the conversion.
- Chrome Asset Library: Pixel Marker — both users of `pixel marker.001` now run live: `.002` matches Blender at 209 / 134 on the authored flat branch and `.003` at 412 / 428 on the authored 3D branch. Recovering curve-point radii and Bezier endpoint tangents brings all six cases for both objects to exact topology and rounded local bounds, including the dense pixel-size controls.
- Chrome Asset Library: Soft Pixel Marker — the 167-node `soft pixel marker` root now runs live with Index Switch, Noise Texture, semi-sharp subdivision, split-edge, and Bezier-tangent support. OpenSubdiv-style sharp non-manifold seams, zero-valued non-manifold Edge Angles, and normalized evaluated-curve tangent bisectors bring all six authored/control cases to exact Blender topology; the authored result is 8,455 vertices / 5,664 faces and local bounds stay within 0.0001 units.
- Chrome Asset Library: `_TEXT.006` — the supplied SF Intermosaic outlines, raised instance extrusion, transform controls, background plate, and authored bounds match Blender's realized 1,373 / 1,135 output exactly. All thirteen validation cases now have exact topology, including Character Spacing 1.5, with bounds within 0.0001 units.
- Chrome Asset Library: 3D String to Text — the `Dojo_STRING TO TEXT.002` graph supports Blender-style editable text, word wrapping, alignment, sizing, spacing, planar curve-normal expansion, N-gon filling, and all 119 reachable node handlers in-browser. All fourteen text, size, alignment, spacing, wrapping, and fill cases now match Blender exactly in topology and rounded local bounds, including the authored 4,446 vertices / 26 faces.
- Chrome Asset Library: String to Text — `Dojo_STRING TO TEXT` is editable in-browser with the packed SF Intermosaic outlines, wrapping, alignment, spacing, all three fill-menu outcomes, and reset-letter behavior. All thirteen validation cases now match Blender exactly in topology and rounded local bounds, including the authored 246 vertices / 20 faces; the extracted `col` and `power` fields also drive the authored emission contract in WebGL.
- Chrome Asset Library: Text Soup — the 406-node `CHROME text.001` root preserves authored Bezier controls, whitespace instance domains, nested glyph transforms, and Smoother Step displacement in-browser. The original nine 1CamBam Stick fonts are missing, so the reproducible comparison substitutes the supplied Pixels Medium TTF in both Blender and the VM. Blender-exact Poly frames, left-associated float32 quaternion products, and a full-precision glyph atlas make all five authored/control cases exact in vertices, faces, and rounded bounds: authored 11,971 / 11,199, edited NODE DOJO 13,242 / 12,432, peak height 11,971 / 11,199, line thickness 9,591 / 8,855, and subdivide 12 at 3,971 / 3,548.
- Chrome Asset Library: Type Pixel Brush Chrome — the 78-node `soft pixel marker.001` root is no longer source-blocked: its missing Budokan font is explicitly replaced by the supplied Pixels Medium TTF in both Blender and the browser. Blender-compatible global N-gon filling restores exact topology across all six validation cases, including the authored 17,860 / 11,296 result, with local bounds within 0.0001 units.
- Chrome Asset Library: Blunt Outline Crayon — the 507-node `blunt outline crayon` root runs with 100% reachable node coverage, including explicit Set Spline Resolution handling for imported evaluated curves and all 665 authored Sigilize blur passes. Canonical closed Mesh-to-Curve loop starts and direction make all five cases exact in topology and rounded bounds, including Blender's authored 24,719 vertices / 22,853 faces.
- Chrome Asset Library: Blunt Metal Marker — the 519-node `blunt metal marker` root is exact in topology and rounded bounds across all six sweeps at Blender's authored 97,691 vertices / 97,669 faces. Region extrusion preserves Blender's unnormalized arithmetic average of selected incident face normals, closing the former non-flattened positional residual.
- Chrome Asset Library: 3D Chrome Crayon Generator — the 979-node `3D CHROME CRAYON generator` root is exact at 161,722 vertices / 88,994 faces and matching rounded bounds across all six Blender cases. Mesh to Curve preserves Blender's repeated pole endpoint for cycles attached to branch poles, accounting for the former 2,752-face instruction deficit. Realize Instances now drops the extraction-only `__font_sample_stride` marker so downstream Fill Curve does not dissolve already-realized glyph samples a second time; legitimate authored curve attributes remain intact.
- N03D 3D-printing Utilities: Dowel Generator — the 42-node `dowel` root runs live with editable diameter, midpoint, cutter, round-square, and cutter-offset controls. All six Blender/VM cases have exact topology and four-decimal bounds, including the authored 333 / 224 result.
- N03D 3D-printing Utilities: Bolt Generator — the 36-group `BOLT GEN DHTS v03_Thru Head_1JUL2024` closure runs live with authored length, thread, head, tap, split, spacer, wall, and preview controls. Float-to-integer Sample Index conversion and a topology-preserving partial annular-prism Exact Boolean reproduce Blender's head bridge, clipped bottom panels, hex recess walls, and step ngons. All six validation cases now have exact topology and local bounds, including the authored 8,646 / 8,535 result, the eight-sided head, thread-only, tap, and spacer modes.
- N03D 3D-printing Utilities: Clevis Pin — the 10-group, 414-node `CLEVIS PIN 31MAY2024_v1` closure matches Blender exactly across all six validation cases. Authored output is 36,390 / 34,198 and Split to Print is 34,548 / 32,094, with identical four-decimal bounds. Blender face winding restores the Solidify head, retained NURBS controls reproduce the exact 2,916 / 974 retaining clip, cyclic Length resampling matches all dimensional controls, and generated Edge Extrude ordering restores the printable shaft split.
- N03D 3D-printing Utilities: Cylindrical Pill Hole — the 33-node `pill hole cylindrical` root matches Blender exactly across five editable diameter and hull-resolution cases. A retained-source two-cylinder Convex Hull path preserves one authored 66-sided cap per end, the 34 exposed points from the second cylinder, all 68 outer wall panels, and Blender's triangle/quad extension tessellation. The authored result is exactly 200 / 176 with identical local bounds.
- N03D 3D-printing Utilities: Countersunk Pill Hole — the 45-node `pill hole countersunk` root matches Blender exactly across all six editable head/shaft diameter, height, depth, and resolution cases. A strict double-precision QuickHull path preserves the four-millionth-unit Z offsets that make all 200 tapered-hull points extreme, then restores Blender's original caps and BEAUTY triangle/quad pairing. The authored stepped cutter is exactly 336 / 362 with identical local bounds.
- N03D 3D-printing Utilities: Hole Punch — the 7-group, 79-node `hole punch` closure matches Blender exactly across six curve/manual-length, head, shaft, height, and resolution cases. The authored result is 153 vertices / 104 faces with identical local bounds.
- N03D 3D-printing Utilities: Hollow Magnet Foot — the 18-node `feet hollow` closure matches Blender's 1,268 / 1,158 topology with local bounds within 0.0001 units. The supplied Selection input is a no-op in Blender, so it is documented rather than exposed as a misleading control.
- N03D 3D-printing Utilities: Arrow Generator — the 4-group, 58-node `arrow nodes` closure matches Blender's 1,155 / 1,153 topology across five tail-length and tail-width cases, with local bounds within 0.0007 units. Preserving its authored edge-domain masks restores the full bent-arrow silhouette rather than the undeformed triangular head.
- N03D 3D-printing Utilities: Glue Grid — the 7-group, 93-node `Geometry Nodes.012` closure honors Blender 5.1 Curve Line Direction/Length mode and reproduces the 4×4 raised lattice. The evaluated mesh is topology-exact at 4,736 vertices / 4,096 faces with bounds within 0.0004 units; point-cloud components remain available during field evaluation but are omitted from the final mesh as Blender does.
- N03D 3D-printing Utilities: Bit Stand — the 5-group, 173-node `BIT STAND nodes` closure matches Blender exactly across all eight validation cases. Authored output is 8,718 / 5,270 and holder-only is 7,374 / 4,556, with identical four-decimal bounds. Blender-compatible Dual Mesh boundaries, linked integer truncation, and Exact planar surface booleans restore the rounded lattice, all 21 circular openings, wall thickness, staggered bit instances, and visibility switch.
- N03D 3D-printing Utilities: Print Bed Previewer — the 7-group, 147-node `Print Bed Previewer.001` closure matches Blender exactly at 10,692 / 95 with identical local bounds across seven cases. The supplied modifier has no print object assigned, making its slice/layer/transform inputs no-ops in Blender as well. The browser preserves the build-plate material assignment, while the comparison renderer intentionally normalizes the shader because the referenced `logo_600x600_crop_center.webp` is absent from the pack; the missing Archivo SemiBold font does not affect this bed-only output.
- N03D 3D-printing Utilities: Patterned Print-Test Sphere — the 4-group, 154-node `Geometry Nodes.002` closure matches Blender exactly at 32,190 / 58,917 with identical local bounds. Mesh Face Area restores the raised per-face tile scale, and Blender-native subdivided Cube vertex/face order makes the two `Index % 2` tessellation branches select the authored alternating faces before Merge by Distance.
- N03D 3D-printing Utilities: Non-Manifold Print Test — the 5-group, 168-node `Geometry Nodes.013` closure matches Blender's Geometry Nodes output exactly at 16,751 / 9,065 with identical bounds, including all diagnostic edge tubes and tetrahedral markers. Mesh to Curve now operates inside instances, Scale Instances is supported, and instance-domain fields work on mixed geometry sets. The saved object applies a separate downstream Bevel modifier (25,719 / 18,182 full-stack), which is documented but excluded from the GN-VM comparison.
- N03D 3D-printing Utilities: Benchy Material Preview — the 3-group, 28-node `benchy` closure preserves the supplied high-resolution Benchy mesh exactly at 112,569 / 225,154 across authored and alternate filament-color cases. This is a material/named-attribute/smooth-shading wrapper rather than a procedural boat generator; the browser preserves geometry and assignment while the comparison renderer normalizes the shader.
- N03D 3D-printing Utilities: Simple Conveyor Drawer — the 2-group, 27-node `simple conveyer` closure shared by `Cube.005` and `Cube.019` matches Blender's canonical user exactly at 16 / 13 across authored, half-close, and closed cases. The editable close/open input translates the drawer over the referenced mesh-line distance; its loose dimension-text curves produce no evaluated mesh faces on either side.
- N03D 3D-printing Utilities: Layer-Height Case Preview — the 14-node `Geometry Nodes.031` tutorial root matches Blender's authored hard-case preview exactly at 4,512 / 3,032 with identical local bounds. Named layer-height attributes drive the face offsets. The comparison uses per-face shading because the triangle soup does not yet carry Blender's split normals, and it normalizes the filament-visualizer shader.
- N03D 3D-printing Utilities: Vice Jaw Profile — the 24-node `Geometry Nodes.028` root matches the standalone `Cube.010` user exactly at 12 / 10 with identical local bounds. Blender's fixed-phase Wave Texture bands and Rotate Vector fields now reproduce both sloped jaw flares; `Cube.008` and `vice plate` reuse the root inside longer modifier stacks.
- N03D 3D-printing Utilities: Mesh-Line Shelf Dowel — the 3-group, 42-node `Shelf_dowel from mesh line` closure matches Blender exactly across six diameter, midpoint, cutter, profile, and offset cases. Authored output is 1,887 / 1,778 with identical local bounds at four-decimal precision. `Plane.001` is an identical saved copy, while `Vert.003` uses the equivalent graph over a longer source line.
- N03D 3D-printing Utilities: Conveyor Mechanic — the 5-group, 75-node `simple conveyor mechanic` closure matches Blender exactly across seven travel, direction, guide, and completion-marker cases. Authored output is 16 / 13 and the fully open marker case is 178 / 169, with local bounds within 0.0006 units. Extracted world matrices preserve the inherited ~1,797× transform on its parented lightbulb marker.
- N03D 3D-printing Utilities: Filleted Print Preview — the 3-group, 46-node `Geometry Nodes.015` demo matches Blender's fixed 1,808 / 905 output and local bounds exactly. Fill Curve now welds the four adjacent tangent duplicates created when Limit Radius clamps neighboring fillets.
- N03D 3D-printing Utilities: Endpoint Guide — the fixed 13-node `Geometry Nodes.007` root converts its supplied two-point line into a beveled guide with square endpoint markers. It matches Blender exactly at 320 / 288 with local bounds within sub-micro-unit rounding. The nearby `Geometry Nodes.016` root on `Plane.007` was audited but its isolated authored output is empty, so it is recorded as a helper rather than published as a generator.
- N03D 3D-printing Utilities: Assembly Bracket — the full 54-group, 1,662-node `asmbly_bracket.001` product is exact across all five validation cases in vertex count, face count, and four-decimal local bounds. The authored result is 1,743 / 1,816; the dense two-cylinder hull, both Exact Boolean cuts, Heal Mesh output, screw assembly, compact-diameter case, and split-for-print branches all reproduce their Blender checkpoints.
- N03D 3D-printing Utilities: Procedural Box — the complete 529-node `Geometry Nodes.003` storage-box assembly matches Blender topology exactly at 90,928 / 87,165 across all eight size, seam, height, animated-lid, and split-pin cases, with every local bound within 0.0001. String-to-Curves outlines retain positional bounds through nested realization so the centered split-print pin rests on the build plane; dense open-shell Exact Booleans retain Blender's collinear dowel-bore boundary samples, and concave wall ngons render without fan-triangulation spikes.
- N03D 3D-printing Utilities: Split n Tap — the intended 43-node `Split n Tap WIP` asset group is live with complete node coverage. The supplied `SPLIT N TAP` wrapper currently renders only a placeholder icosphere, so the reproducible Blender/browser comparison instantiates the nested group directly. Float-to-Integer group truncation, full-range cyclic Trim Curve behavior, and index-stable Fill Curve winding restore the loft seam and top cap. Authored output is topology-exact at 2,372 / 2,380, and all five length/diameter/resolution cases have exact topology with at most 0.0006 units of bound difference. Split/part-selection depend on unassigned Object inputs and evaluate empty in Blender too, so those controls remain hidden until browser object picking is available.
- N03D 3D-printing Utilities: Legacy 3D Chrome Crayon — the 9-group, 250-node `CHROME CRAYON 3D` root matches Blender's authored 71,755 / 71,104 topology and four-decimal bounds exactly after all 1,111 Blur Attribute iterations. A second line-thickness case also matches Blender's four-decimal bounds and all 50,810 faces with one fewer browser vertex.
- N03D 3D-printing Utilities: Curve Crayon — the complete 12-group, 530-node `crayon curve` root matches Blender's authored 1,574 / 1,330 result and local bounds exactly. A changed line-weight case is also exact at 2,828 / 2,597.
- N03D 3D-printing Utilities: Stackable Gridfinity Bin — the complete 14-group, 302-node `bin.stackable` product matches Blender topology across all seven cases. Authored output is 11,288 / 10,410 and optional grid lines are 13,328 / 12,449 on both sides, with bounds within 0.0004 units. Named modifier-attribute inputs preserve the top/inner profile selections, evaluated dependency extraction places all eight feet, crossing cyclic outlines resolve as a planar arrangement, and Blender-style retention of the last collinear fill point closes every swept groove junction.
- N03D active-root closure — all 39 active roots are classified in `public/dojo/n03d/root-classification.json`: 28 published/represented roots, five duplicates or equivalent family variants, and six empty/placeholder/stacked helpers. The final distinct Bolt `.005` watertighten root runs its authored 13-pass manifold heal at a deterministic browser 16,226 / 16,228 versus the stored Blender 16,570 / 16,572 reference. Blender's native OpenVDB result varies between runs; the browser matches 131,278 of 131,279 controlled pass-one SDF signs, and its unchanged 21,642 / 21,818 one-pass topology exactly matches one observed Blender schedule. The remaining 13-pass difference is a compounded scheduling/determinism boundary across repeated remeshing.
- New Joint Generators: Three-Way Pipe — the 20-group, 319-node `pipe joint generator.003` root evaluates its three supplied dowel dependencies in-browser. Authored output and five non-cutter variants match Blender exactly at 48,708 / 48,318 with identical bounds. Cutter mode now routes Blender's compound open-shell FLOAT subtraction through a browser-native open-surface Boolean. It preserves exact bounds and contains every sampled Blender surface point/triangle at zero measured distance, with one small extra inner patch accounting for 3.1% additional surface area.
- New Joint Generators: Modern Pipe Generator — the complete 296-group, 2,413-node `neupipe.001` product runs its six-object collection, instance-domain For Each sleeve builder, SDF Volume Cube surface nets, and both dual-mesh smoothing loops in-browser with no unsupported node types. A direct transient-grid probe found that Blender truncates the linked fractional Volume Cube resolutions to 25 × 24 × 23. Matching that integer coercion makes every authored face exact: 11,163 / 8,064 versus Blender's 11,162 / 8,064, with identical rounded bounds. The second remesh and complete pre-autosmooth shell are count exact at 3,308 / 3,306, and bone preview is exact at 31,447 / 28,386. A direct edge-field probe localizes the last authored delta to one 30-degree auto-smooth selection (125 Blender versus 126 browser), creating one extra referenced fan vertex; size 18 likewise differs by one selection and two fan vertices while retaining every exact face.
- New Joint Generators: Bubble Putty — the 46-group, 828-node `Bubble Putty Generator_9OCT2024_01` closure has full handler coverage. Structure preview is exact at 8,040 / 9,300, the authored 16-bubble stochastic layout is exact at 10,272 / 20,480, and topology remains exact through dynamic triangulation. Full-precision collection payloads reproduce the finalized Boolean envelope. Manifold's only remaining topology delta was one isolated 0.00000335-unit intersection edge whose closed-manifold collapse coalesces the same event as Blender's Exact solver; all three finalized cases now match at 3,302 vertices / 6,608 exported triangles with identical four-decimal bounds (Blender retains 4,675 polygons before triangulation).
- New Joint Generators: Five-Way Pipe Icon — the fixed 2-group, 24-node `pipe icon.001` closure matches Blender exactly at 73,062 / 72,486 with bit-for-bit matching local bounds. The adjacent 23-node `Cylinder.006` root was also audited and intentionally evaluates to empty geometry in both Blender and GN-VM; it is an attribute/material helper, not a missing generator.
- Chrome Asset Library: 3D Chrome Grill Crayon — the complete 1,007-node root is exact in vertex count, face count, and four-decimal local bounds across all four validation cases. Authored and wrapper-enabled output are 61,812 / 53,892, thickness 6 is 42,031 / 33,434, and wrapper 60 by 45 is 38,301 / 33,678. The authored marching, shrink, join, Merge by Distance, three-pass sharpen, and final-position arrays match Blender bit-for-bit. Blender float32 field arithmetic, pinched-polygon weld splitting, radial Split Edges traversal, and preservation of the original vertex block close the final Auto Smooth topology.
- Chrome Asset Library: 2.5D Chrome Crayon — exact Blender counts and four-decimal bounds across all three authored branches: 97,784 / 97,776 for the solid and curve preview, and 48,896 / 46,866 when flattened. Its 508-point Spiro repeat and full 59,872 / 46,866 Marching Squares join also match. Blur Attribute reproduces Blender's float32 accumulation and reciprocal-multiply path across all 665 Sigilize passes, and region extrusion applies Blender's +Z fallback normal to retained zero-area triangles.
- Chrome Asset Library: Spikey Chain Link — the complete 1,748-node closure, including its 256-case Marching Cubes table, is topology-exact at 2,867 / 5,376 with local bounds within 0.0001 units. Blender's cyclic NURBS span order and regenerated evaluated tangents align all 9,324 swept tube points, and face-center weighting in Scale Elements places each cone fan correctly before SDF sampling.
- Math Clay: all thirteen distinct surface roots have live Blender/web comparisons from one shared portable scene dump, complete handler coverage, and the authored silhouette. Twelve roots are topology-exact. Volume Cube and Volume to Mesh match Blender's two-stage OpenVDB lattice, while native Manifold provenance plus polygon-level fan-diagonal cleanup makes lorenze.007 exact at 16,249 / 15,459 and TPMS.018 exact at 14,190 / 13,558 with matching face histograms. TPMS.016 is 87,095 / 85,386 versus the stored Blender 87,099 / 85,390 reference with bounds within 0.0013 units; repeated native OpenVDB 12 runs span both results, identifying the four-count snapshot difference as parallel intersection-tree scheduling rather than a field or browser-topology mismatch.
- The Nodes Node: all twelve distinct active roots now have live comparisons from one shared portable dump. The source cube, dense base plate, paired Hook-deformed noodles, wire stamp guide, and mesh-line noodle are exact or structurally exact. All six 895–900-node UI-panel variants are topology-and-bounds exact with the recovered DejaVu outline atlas. Even-odd N-gon fill leaves glyph counters open while retaining Blender's outline face counts; inclusive cumulative rows, zero-radius control circles, and three-point legacy curve pass-through reproduce the complete panels. Neutral Workbench color now matches, leaving only small glyph-position and lighting/cavity differences.
- Course Modules: all 176 active roots across Intro and Modules 2–4 are structurally inventoried in `public/dojo/course-audit/status.json`. They comprise 113 small instructional snapshots, 16 helpers, and 47 complex studies rather than 176 independent generators. The repeated Modules 2–4 `dojo` finale is live and topology/bounds exact at 47,435 / 46,490. The Intro Geometry Nodes panel diagram, CHALLENGE title, Node Dojo emblem, chalkboard, Node Tabs study, and complete Room are also published with Blender-reference evidence. Module 2's 22 nominal complex roots now have visual closure: one published finale, one earlier Room stage, four single-face marker helpers, and sixteen empty/non-surface lesson roots.
- Send Nodes Hat: all four distinct active roots have exact live Blender/browser topology. The 4-node bill, 29-node front deformation, 40-node sampling cube, and 160-node embroidery graph evaluate at their Blender reference counts; dependency metadata and cycle guarding preserve the evaluated front shell while exact Length-mode sampling restores every stitch and fuzz placement.

## Recommended next targets

1. **Chrome Asset Library** — all 26 active modifier roots now have browser evidence. The collection includes both Pixel Marker users, both Soft Pixel Marker roots, near-exact Text Soup, Blunt Outline Crayon, Blunt Metal Marker, the exact 3D Chrome Grill and 2.5D Chrome Crayon generators, the exact non-surface Mesh to Curve helper, Chain and Mace surface parity, the topology-exact UI Window Generator, both topology-and-bounds-exact editable String to Text roots, exact Type Pixel Brush, and the exact authored Chrome Tools text plate. Only the explicitly documented Text Soup geometry residual remains; shader reconstructions remain renderer approximations unless separately identified as exact contracts.
2. **N03D 3D-printing Utilities** — 73 active objects, 39 roots, 639 groups. All active roots are classified and every distinct product is represented, including the topology-exact Procedural Box and Stackable Bin, both crayon products, the watertight Bolt, and the topology-exact Clevis Pin, Bit Stand, and Split n Tap. Continue only small residual topology refinements on the explicitly documented near-parity products.
3. **New Joint Generators** — 32 active objects, 13 roots, 572 groups. All distinct product roots are now represented: Three-Way Pipe, the 2,413-node modern `neupipe` product, topology-and-bounds-exact Bubble Putty, and the exact Five-Way Pipe Icon. The remaining roots are empty helpers, primitive tests, or duplicates of the existing Chrome/N03D assets; Modern Pipe remains the final surface-parity follow-up in this project.
4. **Math Clay surfaces** — all thirteen distinct surface roots plus the shared Soft Pixel Marker dependency are represented with authored-form parity. Twelve roots are topology-exact, including the dense star cells and D-surface; TPMS.016's four-count difference is within the observed native OpenVDB scheduling range, so remaining work is broader parameter validation rather than a known default-field defect.
5. **The Nodes Node** — all 12 distinct roots are represented in the live library and are exact, structurally exact, or topology-and-bounds exact. Continue only the small glyph-position and Workbench lighting/cavity corrections on the six recovered-font UI variants.
6. **Course Modules** — the complete root audit is recorded in `docs/node-dojo-course-audit.md`. Modules 2–4 now have visual closure in addition to the published Intro studies: the Module 3 control-box exercise is topology-and-bounds exact, while earlier Room stages, one-face markers, and non-surface lesson helpers are classified rather than duplicated in the gallery. Continue only the remaining not-yet-reviewed Intro complex studies.
7. **Send Nodes Hat** — all four distinct roots are represented and topology-exact, including procedural embroidery at 188,934 / 188,160. No geometry correction remains in the current validation set; future work is limited to additional parameter or renderer/material comparisons.

## Needs source or scope clarification

- **Knit Graphic** — 262 Geometry Node groups exist, but no object currently has an active Geometry Nodes modifier.
- **Dusty Crystal Cocoon** — the only active node modifiers reported by Blender are `Auto Smooth`; the authored result appears baked or driven outside the active modifier path.
- **Modules 2–4 and Intro Module** — all 176 active roots are now classified. The 47 complex studies need individual visual-semantic work; the 113 instructional snapshots and 16 helpers are intentionally not expanded into duplicate product entries.
- **Font binaries** — Blurmed, Degular, Dogica, Brokenscript, Pixels, and DejaVu ExtraLight are recovered. Only license-safe Pixels and DejaVu binaries are published; commercial fonts contribute extracted glyph geometry only. Bodoni Poster, Eurostile Bold Extended, and Caslon Black remain unavailable. See `docs/node-dojo-font-audit.md`.
